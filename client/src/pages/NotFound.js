import React from 'react';
import Header from '../components/UI/Header/Header';

const NotFound = () => {
    return (
        <div>
            <Header />
            404
        </div>
    );
};

export default NotFound;